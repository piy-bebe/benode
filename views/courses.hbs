<h1>Courses page</h1>
{{#if courses.length}}
<div class="row">
   {{#each courses}}
   <div class="col s3">
      <div class="card medium">
         <div class="card-image">
            <img src="{{image}}">
         </div>
         <div class="card-content">
            <span class="card-title">{{title}}</span>
            <p class="price">{{price}}</p>
         </div>
         <div class="card-action">
            <a href="/courses/{{id}}" target="_black">CLICK</a>
         </div>
      </div>
   </div>
   {{/each}}
</div>
{{else}}
<p>Courses not found</p>
{{/if}}